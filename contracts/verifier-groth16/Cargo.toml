[package]
name = "verifier-groth16"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "27.0.0-rc.1", default-features = false, features = ["alloc"] }
types = { path = "../types" }

[build-dependencies]
ark-bn254 = "0.4"
ark-ff = "0.4"
num-bigint = "0.5"
serde_json = "1"
