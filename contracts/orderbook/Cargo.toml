[package]
name = "orderbook"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "27.0.0-rc.1", default-features = false, features = ["alloc"] }
types = { path = "../types" }

[dev-dependencies]
soroban-sdk = { version = "27.0.0-rc.1", default-features = false, features = ["testutils"] }

[build-dependencies]
serde_json = "1"
num-bigint = "0.4"
