[package]
name = "ultrahonk-soroban-verifier"
version.workspace = true
edition.workspace = true

[dependencies]
soroban-sdk = { version = "27.0.2", default-features = false }

[features]
default = []
std = []
trace = []
