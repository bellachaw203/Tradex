[package]
name = "ultrahonk-soroban-verifier"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { version = "26.0.1", default-features = false }

[features]
default = []
std = []
trace = []
